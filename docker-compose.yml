services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - PORT=8000
    command: uvicorn src.api:app --host 0.0.0.0 --port 8000 --reload
